import Layout from "../components/Layout";

const index = () => {
  return (
    <Layout>
      <div className="text-2xl underline text-center font-semibold">index</div>
    </Layout>
  );
};

export default index;
